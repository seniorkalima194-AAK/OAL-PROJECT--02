// LoginPage.jsx
// Exports a React component that renders the login form.
// Should use `useAuth` hook to call `login`, validate inputs, and handle errors.
// Form fields: username, password. On success redirect to `DashboardPage`.
