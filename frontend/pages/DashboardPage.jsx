// DashboardPage.jsx
// Student dashboard component. Displays progress summary, recommended lessons/quizzes,
// recent activity, and navigation to subjects and profile. Use `useProgress` and `useAuth`.
